# Environment Variables Template for EcoDu
# Copy this file to .env for local development
# NEVER commit .env to version control

# Django Settings
# Generate a new secret key for production: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-secret-key-here-change-this-in-production

# Debug Mode (set to False in production)
DEBUG=True

# Allowed Hosts (comma-separated, no spaces)
# Local: localhost,127.0.0.1
# Production: yourdomain.com,www.yourdomain.com
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
# Leave empty for local development (uses SQLite)
# Production: Set automatically by Render/Railway
# DATABASE_URL=postgresql://user:password@host:port/database
DATABASE_URL=

# Production Example (Render/Railway will set this automatically):
# DATABASE_URL=postgresql://ecodu_user:password@dpg-xxxxx.oregon-postgres.render.com/ecodu_db
